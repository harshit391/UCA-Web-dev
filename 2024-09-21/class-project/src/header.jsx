export function header() 
{
    let appTitle = "Online Products Store";
    return <h1 style={{textAlign: "center", fontFamily: "Poppins, sans-serif"}}>{appTitle}</h1>; // The Curly Brackets we use while using props in the function is called Interpolation
}